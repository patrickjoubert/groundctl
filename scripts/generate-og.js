#!/usr/bin/env node
// Generates site/og-image.png — 1200x630 dark mode OG image
// Zero external dependencies — pure Node.js PNG encoder

import { writeFileSync } from "fs";
import { createDeflate } from "zlib";
import { fileURLToPath } from "url";
import { dirname, join } from "path";

const __dir = dirname(fileURLToPath(import.meta.url));
const out = join(__dir, "../site/og-image.png");

const W = 1200;
const H = 630;

// ── Pixel buffer (RGBA) ──────────────────────────────────────────────────────
const pixels = new Uint8Array(W * H * 4);

function setPixel(x, y, r, g, b, a = 255) {
  if (x < 0 || x >= W || y < 0 || y >= H) return;
  const i = (y * W + x) * 4;
  pixels[i] = r; pixels[i + 1] = g; pixels[i + 2] = b; pixels[i + 3] = a;
}

function fillRect(x0, y0, w, h, r, g, b, a = 255) {
  for (let y = y0; y < y0 + h; y++)
    for (let x = x0; x < x0 + w; x++)
      setPixel(x, y, r, g, b, a);
}

// ── Minimal bitmap font (5x7 pixel art) ─────────────────────────────────────
// Each char = 5 columns of 7-bit masks (top = MSB)
const FONT = {
  " ": [0,0,0,0,0],
  "g": [0x0E,0x11,0x11,0x0F,0x01,0x11,0x0E],
  "r": [0x16,0x19,0x10,0x10,0x10,0x10,0x10],
  "o": [0x0E,0x11,0x11,0x11,0x11,0x11,0x0E],
  "u": [0x11,0x11,0x11,0x11,0x11,0x13,0x0D],
  "n": [0x11,0x19,0x15,0x13,0x11,0x11,0x11],
  "d": [0x01,0x01,0x01,0x0D,0x13,0x11,0x0F],
  "c": [0x0E,0x11,0x10,0x10,0x10,0x11,0x0E],
  "t": [0x1F,0x04,0x04,0x04,0x04,0x04,0x04],
  "l": [0x18,0x08,0x08,0x08,0x08,0x08,0x1C],
  "A": [0x04,0x0A,0x11,0x11,0x1F,0x11,0x11],
  "w": [0x11,0x11,0x11,0x15,0x15,0x15,0x0A],
  "a": [0x0E,0x11,0x01,0x0F,0x11,0x11,0x0F],
  "y": [0x11,0x11,0x11,0x0F,0x01,0x11,0x0E],
  "s": [0x0E,0x11,0x10,0x0E,0x01,0x11,0x0E],
  "k": [0x11,0x12,0x14,0x18,0x14,0x12,0x11],
  "W": [0x11,0x11,0x11,0x15,0x15,0x0A,0x0A],
  "h": [0x10,0x10,0x10,0x1E,0x11,0x11,0x11],
  "b": [0x10,0x10,0x10,0x1E,0x11,0x11,0x1E],
  "i": [0x0E,0x04,0x04,0x04,0x04,0x04,0x0E],
  "e": [0x0E,0x11,0x11,0x1F,0x10,0x11,0x0E],
  "x": [0x11,0x11,0x0A,0x04,0x0A,0x11,0x11],
  "N": [0x11,0x19,0x19,0x15,0x13,0x13,0x11],
  "m": [0x00,0x1B,0x15,0x15,0x15,0x15,0x11],
  "p": [0x1E,0x11,0x11,0x1E,0x10,0x10,0x10],
  "I": [0x1F,0x04,0x04,0x04,0x04,0x04,0x1F],
  "f": [0x07,0x08,0x08,0x1E,0x08,0x08,0x08],
  "-": [0x00,0x00,0x00,0x1F,0x00,0x00,0x00],
  "@": [0x0E,0x11,0x17,0x15,0x17,0x10,0x0E],
  "/": [0x01,0x01,0x02,0x04,0x08,0x10,0x10],
  ".": [0x00,0x00,0x00,0x00,0x00,0x0C,0x0C],
  "·": [0x00,0x00,0x00,0x0C,0x0C,0x00,0x00],
  "→": [0x00,0x04,0x08,0x1F,0x08,0x04,0x00],
  "M": [0x11,0x1B,0x15,0x15,0x11,0x11,0x11],
  "L": [0x10,0x10,0x10,0x10,0x10,0x10,0x1F],
  "T": [0x1F,0x04,0x04,0x04,0x04,0x04,0x04],
  "O": [0x0E,0x11,0x11,0x11,0x11,0x11,0x0E],
  "P": [0x1E,0x11,0x11,0x1E,0x10,0x10,0x10],
  "S": [0x0E,0x11,0x10,0x0E,0x01,0x11,0x0E],
  "E": [0x1F,0x10,0x10,0x1E,0x10,0x10,0x1F],
  "C": [0x0E,0x11,0x10,0x10,0x10,0x11,0x0E],
  "R": [0x1E,0x11,0x11,0x1E,0x14,0x12,0x11],
  "K": [0x11,0x12,0x14,0x18,0x14,0x12,0x11],
  "X": [0x11,0x0A,0x04,0x04,0x04,0x0A,0x11],
  "v": [0x11,0x11,0x11,0x11,0x0A,0x0A,0x04],
  "j": [0x03,0x01,0x01,0x01,0x01,0x11,0x0E],
  "p": [0x1E,0x11,0x11,0x1E,0x10,0x10,0x10],
  "q": [0x0F,0x11,0x11,0x0F,0x01,0x01,0x01],
  "z": [0x1F,0x01,0x02,0x04,0x08,0x10,0x1F],
  ":": [0x00,0x0C,0x0C,0x00,0x0C,0x0C,0x00],
};

function drawText(text, x, y, scale, r, g, b) {
  let cx = x;
  for (const ch of text) {
    const glyph = FONT[ch] || FONT[" "];
    for (let row = 0; row < 7; row++) {
      const mask = glyph[row] ?? 0;
      for (let col = 0; col < 5; col++) {
        if (mask & (1 << (4 - col))) {
          fillRect(cx + col * scale, y + row * scale, scale, scale, r, g, b);
        }
      }
    }
    cx += 6 * scale;
  }
}

// ── Draw image ───────────────────────────────────────────────────────────────

// Background
fillRect(0, 0, W, H, 13, 13, 13);

// Subtle grid
for (let x = 0; x < W; x += 60) fillRect(x, 0, 1, H, 22, 22, 22);
for (let y = 0; y < H; y += 60) fillRect(0, y, W, 1, 22, 22, 22);

// Green top bar
fillRect(0, 0, W, 6, 0, 255, 136);

// "groundctl" — large white, centered
const title = "groundctl";
const tScale = 12;
const tW = title.length * 6 * tScale;
const tX = Math.floor((W - tW) / 2);
drawText(title, tX, 160, tScale, 255, 255, 255);

// Tagline — green
const tag = "Always know what to build next.";
const tagScale = 4;
const tagW = tag.length * 6 * tagScale;
const tagX = Math.floor((W - tagW) / 2);
drawText(tag, tagX, 340, tagScale, 0, 255, 136);

// npm install line — mono green, smaller
const cmd = "npm install -g @groundctl/cli";
const cmdScale = 3;
const cmdW = cmd.length * 6 * cmdScale;
const cmdX = Math.floor((W - cmdW) / 2);
drawText(cmd, cmdX, 430, cmdScale, 0, 200, 100);

// Bottom label
const lbl = "groundctl.org  ·  MIT  ·  open source";
const lblScale = 2;
const lblW = lbl.length * 6 * lblScale;
const lblX = Math.floor((W - lblW) / 2);
drawText(lbl, lblX, 575, lblScale, 60, 60, 60);

// ── PNG encoder ──────────────────────────────────────────────────────────────
function adler32(buf) {
  let s1 = 1, s2 = 0;
  for (const b of buf) { s1 = (s1 + b) % 65521; s2 = (s2 + s1) % 65521; }
  return (s2 << 16) | s1;
}

function crc32(buf) {
  const table = new Uint32Array(256);
  for (let i = 0; i < 256; i++) {
    let c = i;
    for (let k = 0; k < 8; k++) c = (c & 1) ? (0xEDB88320 ^ (c >>> 1)) : (c >>> 1);
    table[i] = c;
  }
  let crc = 0xFFFFFFFF;
  for (const b of buf) crc = table[(crc ^ b) & 0xFF] ^ (crc >>> 8);
  return (crc ^ 0xFFFFFFFF) >>> 0;
}

function u32be(n) {
  return [(n >>> 24) & 0xFF, (n >>> 16) & 0xFF, (n >>> 8) & 0xFF, n & 0xFF];
}

function chunk(type, data) {
  const typeBytes = [...type].map(c => c.charCodeAt(0));
  const crc = crc32([...typeBytes, ...data]);
  return [...u32be(data.length), ...typeBytes, ...data, ...u32be(crc)];
}

// IHDR
const IHDR = chunk("IHDR", [
  ...u32be(W), ...u32be(H),
  8,  // bit depth
  2,  // color type = RGB
  0, 0, 0
]);

// Raw scanlines (filter byte 0 = None per row)
const raw = new Uint8Array(H * (1 + W * 3));
for (let y = 0; y < H; y++) {
  raw[y * (1 + W * 3)] = 0; // filter None
  for (let x = 0; x < W; x++) {
    const pi = (y * W + x) * 4;
    const ri = y * (1 + W * 3) + 1 + x * 3;
    raw[ri] = pixels[pi]; raw[ri + 1] = pixels[pi + 1]; raw[ri + 2] = pixels[pi + 2];
  }
}

// Compress with zlib (sync)
import { deflateSync } from "zlib";
const compressed = deflateSync(raw, { level: 6 });

const IDAT = chunk("IDAT", [...compressed]);
const IEND = chunk("IEND", []);

const PNG_SIG = [137, 80, 78, 71, 13, 10, 26, 10];
const pngBytes = new Uint8Array([...PNG_SIG, ...IHDR, ...IDAT, ...IEND]);

writeFileSync(out, pngBytes);
console.log(`✓ og-image.png generated (${(pngBytes.length / 1024).toFixed(1)} KB) → ${out}`);
